// Thin composable wrapper — delegates to the Pinia prompt store.
// Call setTemplateId(id) to switch between templates.
export { usePromptStore as usePromptCustomization } from '@/stores/promptStore';
