// Thin composable wrapper — delegates to the Pinia tool-instance store.
export { useToolInstanceStore as useToolInstances } from '@/stores/toolInstanceStore';
export type { ToolInstancesManager } from '@/stores/toolInstanceStore';
